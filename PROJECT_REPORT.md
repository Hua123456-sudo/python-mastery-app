# PROJECT_REPORT

## 1. 项目背景
在 Python 课程学习过程中，单纯的代码练习往往难以同时体现交互能力、数据管理能力和算法展示能力。为了满足课程项目“本地运行、使用 Python 与 pygame、包含题库、用户、结果分析、题库管理和算法可视化”的要求，本项目设计并实现了一个完整的桌面交互式学习系统 `Python Mastery`。

该系统既可以作为学生练习平台，也可以作为课堂展示工具。项目重点不仅在于功能实现，还包括统一的界面风格、可靠的数据存储方式和适合答辩展示的演示流程。

## 2. 项目目标
- 实现一个基于 Python 的本地交互式学习应用
- 支持用户登录、测验练习、结果展示和学习分析
- 支持管理员维护题库内容
- 提供数组、链表、树三类算法可视化
- 使用统一的深色现代化 UI 提升展示效果
- 保证项目稳定性，适合课程答辩和现场演示

## 3. 功能模块说明

### 3.1 登录 / 注册模块
- 支持用户注册与登录
- 区分 `student` 与 `admin` 两种角色
- 密码使用 SHA-256 哈希保存
- 默认管理员账号自动初始化，方便测试与演示

### 3.2 题库模块
- 题目存储于本地 `questions.json`
- 题目包含 `id、type、category、difficulty、question、options、answer、explanation、source`
- 支持单选题、填空题、程序输出题

### 3.3 测验模块
- 用户可按题目数量、难度和分类配置测验
- 系统按条件随机抽题
- 每次显示 1 道题
- 提交后即时反馈正确与错误，并显示解析

### 3.4 结果模块
- 测验完成后进入结果页
- 展示分数、正确题数、总题数、正确率和用时
- 同时保存本次测验结果到历史文件

### 3.5 学习分析模块
- 从历史记录中提取当前用户的数据
- 展示平均分、最佳成绩、总测验次数、最近一次结果
- 绘制成绩趋势图、分类正确率图和难度分布图

### 3.6 管理员题库管理模块
- 仅管理员可进入
- 支持查看、搜索、筛选、新增、编辑、删除题目
- 修改后直接写回本地题库文件

### 3.7 算法可视化模块
- Arrays：线性查找
- Lists：链表插入
- Trees：二叉树中序遍历
- 支持播放、暂停、下一步和重置控制

### 3.8 Lesson 模块
- 当前版本为课程内容页占位实现
- 已统一到项目整体风格中，便于后续继续扩展章节内容

## 4. 关键技术设计

### 4.1 架构设计
项目采用分层结构：
- `ui`：负责页面展示与交互
- `services`：负责业务逻辑
- `models`：负责数据模型
- `storage`：负责本地文件读写
- `utils`：提供安全数据处理与辅助能力

这种结构有助于把页面逻辑与业务逻辑分开，使系统更清晰、更容易维护。

### 4.2 页面路由设计
系统使用 `Router` 统一管理页面注册与切换。  
当前主要页面包括：
- LoginScreen
- HomeScreen
- LessonScreen
- QuizScreen
- ResultScreen
- AnalyticsScreen
- AdminScreen
- AlgorithmScreen

### 4.3 数据安全设计
为了提高稳定性，项目实现了安全 JSON 读写封装：
- 文件不存在时自动初始化
- 文件为空时自动恢复默认结构
- 文件内容格式错误时自动回退到安全数据
- 根结构不合法时进行保护

### 4.4 权限控制设计
- `student` 只能使用学习相关功能
- `admin` 额外拥有题库管理权限
- 首页入口和页面进入时都进行角色校验

## 5. 数据存储设计

### 5.1 users.json
保存用户数据，字段包括：
- `username`
- `password_hash`
- `role`

### 5.2 questions.json
保存运行中的题库数据，字段包括：
- `id`
- `type`
- `category`
- `difficulty`
- `question`
- `options`
- `answer`
- `explanation`
- `source`

### 5.3 questions_seed.json
作为稳定题库种子文件，用于在运行题库损坏时恢复默认题库。

### 5.4 quiz_history.json
保存历史测验记录，字段包括：
- `record_id`
- `username`
- `quiz_date`
- `score`
- `correct_count`
- `total_questions`
- `accuracy`
- `time_spent`
- `difficulty`
- `category`
- `wrong_questions`

## 6. UI 设计说明
项目整体采用深色、现代、卡片式风格，主要考虑：
- 课堂投屏时对比度更高
- 多模块页面风格统一
- 不依赖图片和声音，符合课程要求

本次 UI 设计重点包括：
- 统一标题层级
- 统一按钮尺寸与圆角
- 统一卡片和信息面板样式
- 增加状态提示、空状态和进度条视觉规范

## 7. exercises.py 题目来源说明
当前仓库中的 `exercises/exercises.py` 仍为占位文件。  
为了满足课程项目对题目来源与函数算法题的要求，题库扩展阶段是根据课程要求中列出的函数名列表构建的，并在题目 `source` 字段中保留了函数来源标记，例如：
- `isPrime`
- `isPalindrome`
- `rotateLeft`
- `wordCounter`

因此，当前版本能够清楚展示题目与函数来源的对应关系，但后续如果补齐真实 `exercises.py` 源码，还可以继续做更严格的自动映射。

## 8. 遇到的问题与解决方法

### 8.1 JSON 文件异常导致程序崩溃
问题：
- 文件为空或格式错误时，程序可能在读取时异常

解决：
- 增加安全 JSON 读写工具
- 自动初始化和默认结构回退

### 8.2 页面切换后的状态残留
问题：
- 用户退出登录后，某些页面仍可能保留上一次状态

解决：
- 在 `BaseScreen` 中增加统一状态清理接口
- 在 `App.logout()` 中统一调用各页面清理逻辑

### 8.3 管理员误写入非法题目
问题：
- 单选题可能出现答案不在选项中的情况

解决：
- 在 `QuestionService` 中统一做字段规范化和合法性校验

### 8.4 题库文件损坏后无法恢复
问题：
- 如果运行中的 `questions.json` 被改坏，题库可能为空

解决：
- 增加 `questions_seed.json` 作为稳定种子来源

## 9. AI 在项目中的作用
AI 在本项目中主要起到了以下辅助作用：
- 生成项目骨架与页面初稿
- 辅助编写业务层逻辑
- 协助补全文档内容
- 辅助发现边界问题并提出修复思路

但项目中的功能确认、结构整理、手动测试、代码整合和最终提交材料整理，仍需要开发者自己完成。AI 是开发辅助工具，而不是完全替代开发者的主体工作。

## 10. 项目总结与反思
本项目最终完成了一个具备较完整功能闭环的本地交互式学习系统，满足了课程项目中的以下关键要求：
- Python + pygame + pygame_gui
- 本地运行
- JSON 存储
- 用户登录
- 题库测验
- 结果统计
- 图表分析
- 管理员题库管理
- 算法可视化

从开发过程来看，本项目最大的收获在于：
- 学会了如何把界面、业务逻辑和数据模型分层组织
- 学会了如何为课堂演示设计稳定的流程
- 学会了如何把 AI 作为辅助开发工具，而不是简单复制生成结果

如果后续继续迭代，本项目还可以进一步增强课程内容模块、题库自动生成能力和更丰富的算法演示效果。
