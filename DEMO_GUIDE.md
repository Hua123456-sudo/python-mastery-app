# DEMO_GUIDE

## 文档用途
本文件用于课堂展示、项目答辩和功能演示准备，帮助你在有限时间内按最稳妥的顺序展示系统，并提前准备老师可能追问的问题。

## 一、课堂演示推荐顺序
建议总时长控制在 6 到 10 分钟。

### 1. 打开项目并登录
- 启动程序
- 使用默认管理员账号登录：
  - 用户名：`admin`
  - 密码：`admin123`

你可以这样讲：
“这个系统是一个本地运行的 Python 学习应用，包含登录、题库测验、结果分析、题库管理和算法可视化几个模块，适合课程学习和课堂演示。”

### 2. 首页总览
- 展示首页的功能入口
- 指出当前账号角色
- 说明不同模块的用途

你可以这样讲：
“首页相当于系统工作台，用户可以从这里进入测验、学习分析、算法可视化，管理员还可以进入题库管理。”

### 3. Quiz 测验演示
- 点击“开始测验”
- 选择：
  - 题目数量：`10`
  - 难度：`all`
  - 分类：`all`
- 开始测验
- 现场做 2 到 3 题
- 至少故意做错 1 题，方便展示解释与结果差异

你可以这样讲：
“测验开始前可以按数量、难度和分类筛选题目。系统会从本地 JSON 题库中随机抽题，每次显示 1 道题，提交后立刻反馈对错和解析。”

### 4. ResultScreen 结果页
- 完成一轮测验或快速点到最后
- 展示：
  - 分数
  - 正确题数
  - 正确率
  - 用时
  - 结果保存提示

你可以这样讲：
“测验结束后，系统会自动计算成绩，并把结果写入本地历史文件，为后面的分析图表做准备。”

### 5. AnalyticsScreen 学习分析
- 点击进入学习分析
- 展示：
  - 平均分
  - 最佳成绩
  - 总测验次数
  - 最近一次结果
  - 成绩趋势图
  - 分类正确率柱状图
  - 难度分布图

你可以这样讲：
“这个页面的重点是把单次测验结果转换为长期学习分析，帮助用户观察自己的成绩变化趋势和不同分类下的表现。”

### 6. AlgorithmScreen 算法可视化
- 返回首页
- 进入算法可视化
- 依次点：
  - Arrays
  - Lists
  - Trees
- 演示 `Play / Pause / Next / Reset`

你可以这样讲：
“课程项目要求包含算法可视化，所以我用 pygame 原生绘图实现了数组线性查找、链表插入和二叉树中序遍历的过程展示。”

### 7. AdminScreen 题库管理
- 从首页进入管理员页面
- 演示：
  - 搜索题目
  - 按难度或分类筛选
  - 点开一题查看详情
  - 修改一题或新建一题

你可以这样讲：
“管理员可以直接维护 `questions.json`，包括搜索、筛选、新增、编辑和删除题目，系统会自动校验字段合法性并写回本地题库。”

### 8. LessonScreen 收尾
- 简短打开课程页
- 说明目前是占位页

你可以这样讲：
“课程页当前是统一风格占位版本，后续可以继续扩展章节内容、代码示例和知识点讲解。”

## 二、每一步点击什么
### 登录
- 启动 `main.py`
- 输入 `admin / admin123`
- 点击“立即登录”

### 测验
- 首页点击“开始测验”
- 选择 `10 / all / all`
- 点击“开始测验”
- 做题并提交

### 结果
- 题目结束后自动跳转
- 点击“返回首页”或“再来一次”

### 分析
- 首页点击“学习分析”

### 算法
- 首页点击“算法可视化”
- 左侧切换算法
- 右侧点击控制按钮

### 管理员后台
- 首页点击“Admin”
- 左侧搜索和筛选
- 中间修改表单
- 点击“保存”

## 三、每个功能该讲什么
### 登录 / 注册
- 角色区分：`student` 与 `admin`
- 密码不明文保存，使用 SHA-256 哈希
- 数据持久化在本地 `users.json`

### Quiz
- 支持三种题型：单选、填空、程序输出
- 支持随机抽题与筛选条件
- 提交后即时反馈

### Result
- 展示单次测验表现
- 同时作为历史记录写入入口

### Analytics
- 展示长期学习趋势
- 强调“图表分析”是项目亮点之一

### Admin
- 展示系统不仅能做题，还能维护内容
- 强调对课堂项目来说，这说明系统是完整产品，而不只是单个测验页面

### Algorithm
- 强调课程项目要求中的“算法可视化”
- 说明未使用图片资源，而是用 pygame 原生绘图实现

## 四、如何简洁介绍系统架构
推荐回答：

“系统整体采用本地桌面应用结构。`App` 负责程序启动和全局状态，`Router` 负责页面切换，`ui` 目录下是各个界面页面，`services` 负责业务逻辑，比如认证、题库、测验、历史记录和分析，`models` 定义数据结构，`storage` 负责 JSON 安全读写。这样界面逻辑和业务逻辑是分开的，便于维护和扩展。”

## 五、老师可能会问的问题
### 1. 为什么选择 pygame 而不是 Web？
可以回答：
“因为课程要求明确使用 Python、pygame 和 pygame_gui，并且系统只需本地运行，不需要服务器，所以桌面实现更符合项目要求。”

### 2. 数据为什么放在 JSON？
可以回答：
“题库、用户和历史记录都属于规模较小、结构清晰的数据，本地 JSON 易于调试、易于展示，也符合课程项目的轻量化要求。”

### 3. 如何保证密码安全？
可以回答：
“密码没有明文保存，而是用 `hashlib.sha256` 做哈希后写入 `users.json`。虽然这不是生产环境最强方案，但对课程项目已经体现了基本安全意识。”

### 4. 题库是怎么组织的？
可以回答：
“每道题有 `id、type、category、difficulty、question、options、answer、explanation、source` 等字段，便于前端展示、筛选和后续统计分析。”

### 5. 如果 JSON 文件损坏怎么办？
可以回答：
“我在底层做了安全读写封装，空文件、非法 JSON 或错误结构时会自动回落到默认数据结构，避免程序直接崩溃。”

### 6. student 为什么不能进入管理员页面？
可以回答：
“因为系统有角色权限控制。首页只对 admin 显示入口，进入页面时也会再次检查角色，避免 student 手动跳转进入。”

### 7. 图表是怎么实现的？
可以回答：
“分析图表是用 pygame 原生绘图实现的，没有依赖图片资源。趋势图、柱状图和难度分布图都由历史数据实时计算后绘制。”

### 8. 算法可视化为什么做这三类？
可以回答：
“数组、链表、树是课程中最典型的三类结构，分别对应查找、插入和遍历场景，适合课堂讲解和可视化展示。”

## 六、老师可能追问的代码理解问题
### 页面切换怎么做的？
- `Router` 持有所有 screen
- `navigate()` 时先退出当前页，再进入目标页

### 测验逻辑怎么拆分的？
- `QuizScreen` 只负责交互展示
- `QuizService` 负责抽题、判题、进度和结果汇总

### 历史记录怎么保存？
- `QuizHistoryService` 负责把单次测验结果组装成记录并写入 `quiz_history.json`

### Analytics 数据怎么来的？
- `AnalyticsService` 从当前用户历史记录中计算平均分、最佳成绩、趋势点、分类正确率和难度分布

### 管理员编辑题目时如何防止非法数据？
- `QuestionService` 在保存前统一做字段规范化和校验
- 例如单选题至少两个选项、答案必须在选项里、难度必须合法

## 七、如何解释 AI 辅助开发过程
推荐回答：

“我把 AI 作为开发辅助工具，主要用于生成项目骨架、补充界面代码、整理文档、帮助检查边界情况和修复 Bug。但整体模块规划、功能确认、逻辑调整、手动验证和最终整合是我自己完成的。我也保留了开发记录模板，用于记录每次 AI 辅助的内容和我手动修改的部分。”

## 八、如何展示 exercises.py 题目来源
推荐展示方式：
- 打开 `data/questions.json`
- 找几道题的 `source` 字段
- 展示它们对应的函数名，例如 `isPrime`、`rotateLeft`、`wordCounter`

建议说明：

“当前仓库中的 `exercises.py` 还是占位文件，但题库扩展是基于课程要求给出的函数名列表生成的，并在 `source` 字段中保留了函数来源标记。后续如果补齐真实函数实现，可以继续把题库和源码更严格对应起来。”

## 九、如何突出项目亮点
你可以重点强调四个亮点：

### 1. UI 完整度
- 深色现代化风格统一
- 首页、测验、分析、后台和算法页风格一致

### 2. 图表分析
- 不只是做题，还能看成绩变化和分类表现

### 3. 算法可视化
- 满足课程要求
- 适合课堂演示

### 4. 管理员后台
- 说明系统是完整产品闭环
- 支持内容维护，不只是静态题库

## 十、默认演示流程
最稳的推荐顺序：
1. 登录
2. 首页
3. Quiz
4. Result
5. Analytics
6. Algorithm
7. Admin
8. Lesson 占位说明

如果时间很短：
1. 登录
2. Quiz 做两题
3. Result
4. Analytics
5. Algorithm

## 十一、答辩时的收尾总结
推荐收尾表达：

“这个项目完成了从用户登录、题库测验、结果保存、学习分析到管理员题库维护和算法可视化的一整套本地学习系统闭环，既满足了课程功能要求，也兼顾了界面完整性、稳定性和课堂展示效果。”
